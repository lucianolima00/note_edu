import React, {useHistory} from 'react';

export const AddReminder = ({setUsername}) => {
  const [error, setError] = useState();
  function handleSubmit(event){
      event.preventDefault();
  }
    return <div></div>;
};
